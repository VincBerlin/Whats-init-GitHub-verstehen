# Required for LLM-based repository analysis
GEMINI_API_KEY=your_gemini_api_key

# Optional but strongly recommended in production to avoid GitHub API rate limits
GITHUB_TOKEN=your_github_personal_access_token
